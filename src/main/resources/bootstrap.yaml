spring:
  cloud:
    propertiesvolume:
      prefixed: false
      paths: /mnt/secrets/aac
      aliases:
        aac-manage-case-s2s-secret: MANAGE_CASE_S2S_KEY
        AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
